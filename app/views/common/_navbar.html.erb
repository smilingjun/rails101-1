<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#">Rails BootStrap4</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto">
      <% if !current_user %>
      <li class="nav-item"><%= link_to("注册", new_user_registration_path, class: "nav-link active") %> </li>
      <li class="nav-item"><%= link_to("登入", new_user_session_path, class: "nav-link active") %></li>
      <% end %>
      <li class="nav-item dropdown">
        <% if current_user %>
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <%= t("hi") %>, <%= current_user.email %>
        </a>

        <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
          <%= link_to("登出", destroy_user_session_path, method: :delete, class: "dropdown-item") %>
        <% end %>
        </div>
      </li>
      </ul>
    </div>
</nav>
